//  {{ prefix }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright {{ year }} {{ developer.company }}. All rights reserved.

import UIKit

final class {{ prefix }}{{ module_info.name }}Builder: ModuleBuilder {

    var initialState: {{ prefix }}{{ module_info.name }}.ViewControllerState?

    func set(initialState: {{ prefix }}{{ module_info.name }}.ViewControllerState) -> {{ prefix }}{{ module_info.name }}Builder {
        self.initialState = initialState
        return self
    } 

    func build() -> UIViewController {
        let presenter = {{ prefix }}{{ module_info.name }}Presenter()
        let interactor = {{ prefix }}{{ module_info.name }}Interactor(presenter: presenter)
        let controller = {{ prefix }}{{ module_info.name }}ViewController(interactor: interactor)

        presenter.viewController = controller
        return controller
    }
}
